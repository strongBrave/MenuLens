# ═══════════════════════════════════════════════════════════════════
# MenuGen Backend 环境变量示例
# ═══════════════════════════════════════════════════════════════════
# 复制为 .env 后按需修改
# cp .env.example .env

# =============================================================================
# 🔍 搜索服务基础配置
# =============================================================================

# 搜索服务提供商: "serpapi" 或 "google"
SEARCH_PROVIDER="serpapi"

# SerpAPI 搜索引擎（SEARCH_PROVIDER=serpapi 时使用）
SERPAPI_ENGINE="google"

# Google Custom Search 配置（SEARCH_PROVIDER=google 时使用）
# SEARCH_API_KEY=""
# SEARCH_ENGINE_ID=""

# 搜索超时时间 (秒)
SEARCH_TIMEOUT=60

# 每个菜品最终返回图片数量
SEARCH_NUM_RESULTS=1

# 最大并发搜索数（影响图片加载速度）
MAX_CONCURRENT_SEARCHES=5

# =============================================================================
# 🖼️ 图片处理基础配置
# =============================================================================

# 图片 URL 可访问性检查超时 (秒)
IMAGE_URL_CHECK_TIMEOUT=5

# 图片验证请求超时 (秒)
IMAGE_VERIFY_TIMEOUT=30

# =============================================================================
# 🌐 服务配置
# =============================================================================

BACKEND_HOST=0.0.0.0
BACKEND_PORT=8000

# 前端开发地址
CORS_ORIGIN=http://localhost:5173

# =============================================================================
# 📁 文件上传配置
# =============================================================================

MAX_FILE_SIZE_MB=10

# =============================================================================
# 🔧 高级配置
# =============================================================================

# HTTP 代理（可选）
# PROXY_URL="http://127.0.0.1:7897"

# 是否在启动时验证配置
VALIDATE_SETTINGS=false

# =============================================================================
# ℹ️ 说明
# =============================================================================
# 以下参数已改为前端设置面板运行时传入，不再要求写在 .env：
# - LLM 相关（key/base_url/model/temperature/timeout）
# - SerpAPI Key
# - SEARCH_CANDIDATE_RESULTS
# - RAG 开关与阈值
# - 图片生成开关、Key、Model
